# Auto-generated by gafrojs scripts/publish-pages.sh — do not edit here; edit the generator.
# Site is committed at the repo root and built locally (CI cannot build the wasm). This job
# resolves Git LFS and publishes the tree to GitHub Pages.
name: Deploy site to GitHub Pages
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: true
jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (with LFS)
        uses: actions/checkout@v4
        with:
          lfs: true            # materialize gafrojs.data/.wasm from LFS
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .              # the whole repo root is the site
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
